%home
  %img{:src => "#{@picture.image_link}"}
  %nav
    %ul
      %li= link_to "View Portfolio", user_path(@show_user)
  %arrows
    %left
      = link_to("", root_path(id: @prev_picture))
    %right
      = link_to("", root_path(id: @next_picture))
