%home
  %img{:src => "#{@image.image_link}"}
  %nav
    %ul
      %category #{@image.category.name}:
      %li= link_to  "View Portfolio", root_path
      <br/>
      %li= link_to "Prev | ", root_path(cat: @prev)
      %li= link_to "Next", root_path(cat: @next)